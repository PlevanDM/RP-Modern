╔═══════════════════════════════════════════════════════════════╗
║     ОНОВІТЬ СЕРВЕР ЗАРАЗ                                      ║
╚═══════════════════════════════════════════════════════════════╝

Виконайте ці команди:

ssh root@70.34.252.148
# Пароль: 8zU%)m9$eVu-$wHd

Після входу:

cd /root/repair-hub-pro
git pull origin eploy
docker compose down
docker compose build --no-cache
docker compose up -d
sleep 20
docker compose ps
docker logs repair-hub-pro --tail=30

═══════════════════════════════════════════════════════════════

АБО В ОДНУ ЛІНІЮ:

ssh root@70.34.252.148 "cd /root/repair-hub-pro && git pull origin eploy && docker compose down && docker compose build --no-cache && docker compose up -d && sleep 20 && docker compose ps"

═══════════════════════════════════════════════════════════════

ПІСЛЯ ОНОВЛЕННЯ:

⏳ Чекайте 5-30 хвилин - Cloudflare отримає nameservers зміни
📧 Перевірте email - ви отримаєте "Domain is active" від Cloudflare
🌐 Після цього відкрийте: http://repairhub.one

═══════════════════════════════════════════════════════════════

🔒 БОНУС - HTTPS (коли http працює):

ssh root@70.34.252.148
apt update
apt install -y certbot python3-certbot-nginx
certbot --nginx -d repairhub.one -d www.repairhub.one

Тоді: https://repairhub.one

═══════════════════════════════════════════════════════════════

